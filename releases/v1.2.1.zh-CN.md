# Codex Now v1.2.1

## Bug 修复

- 修复：第二次启动 app 无反应的问题
- 原因：macOS 上 `mktemp /tmp/file-XXXXXX` 格式不会展开随机后缀，导致临时文件名冲突，第二次启动直接报错退出
- 修复方案：改用 `mktemp -t` 格式，macOS 上能正确生成唯一临时文件名

## 安装 / 升级

```bash
curl -fsSL https://raw.githubusercontent.com/xiaolin26/Codex-now/main/install.sh | bash
```
